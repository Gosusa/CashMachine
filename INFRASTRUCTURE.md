# 인프라 & 서버 구성 가이드

> 서비스 규모별 서버 구성, 기술 선택, 확장 전략 정리.

---

## 서비스 아키텍처 개요

### 역할 분리

| 역할 | 설명 | 특성 |
|---|---|---|
| 웹 앱 | 챗 UI, 대시보드, 시뮬레이션, 결제 | 빠른 응답, 가벼움 |
| DB | 사용자, 포트폴리오, 세션, 주가, 리서치 | 항상 접근 가능 |
| 크롤링 워커 | 주가 수집, 뉴스 크롤링, AI 판정 | 스케줄 실행, 1~10분 소요 |
| AI API | 챗 응답, 시뮬레이션 추론 | BYOK, 스트리밍 |
| 결제 | 구독 관리, 웹훅 | 외부 서비스 연동 |

### 전체 구조도

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   사용자 (브라우저/모바일)                                    │
│         │                                                   │
│         ▼                                                   │
│   ┌───────────────────────────────────┐                     │
│   │         Vercel (무료→$20)          │                     │
│   │                                   │                     │
│   │  Next.js App                      │                     │
│   │  ├ 페이지 렌더링 (SSR)             │                     │
│   │  ├ /api/auth     ← 로그인         │                     │
│   │  ├ /api/chat     ← AI 스트리밍     │                     │
│   │  ├ /api/simulate ← 시뮬레이션 계산 │                     │
│   │  ├ /api/session  ← 세션 저장/조회  │                     │
│   │  └ /api/payment  ← 결제 웹훅      │                     │
│   │                                   │                     │
│   └──────────────┬────────────────────┘                     │
│                  │ 읽기/쓰기                                 │
│                  ▼                                          │
│   ┌───────────────────────────────────┐                     │
│   │      Supabase (무료→$25)           │                     │
│   │                                   │                     │
│   │  PostgreSQL + Auth                │                     │
│   │  ├ users          (사용자 정보)    │                     │
│   │  ├ portfolios     (포트폴리오)     │                     │
│   │  ├ simulations    (저장된 세션)    │                     │
│   │  ├ stock_data     (일일 주가)      │                     │
│   │  ├ base_data      (리서치 압축본)  │                     │
│   │  ├ user_overrides (사용자 커스텀)  │                     │
│   │  ├ news_alerts    (뉴스 판정 결과) │                     │
│   │  └ chat_history   (대화 기록)      │                     │
│   │                                   │                     │
│   └──────────────┬────────────────────┘                     │
│                  │ 읽기/쓰기                                 │
│                  ▼                                          │
│   ┌───────────────────────────────────┐                     │
│   │     GCP Cloud Run (무료 티어)      │                     │
│   │                                   │                     │
│   │  Python 워커 (스케줄 실행)         │                     │
│   │  ├ 매일 07:00 — 주가 수집 (1분)    │                     │
│   │  ├ 매일 07:05 — 뉴스 크롤링 (5분)  │                     │
│   │  ├ 매일 07:15 — AI 리스크 판정     │                     │
│   │  └ 결과 → DB 저장                 │                     │
│   │                                   │                     │
│   │  Cloud Scheduler (cron 트리거)     │                     │
│   └───────────────────────────────────┘                     │
│                                                             │
│   ┌───────────────────────────────────┐                     │
│   │     외부 서비스                     │                     │
│   │  ├ Gemini/Claude API (AI, BYOK)   │                     │
│   │  ├ yfinance (주가 데이터)          │                     │
│   │  ├ Toss Payments (결제)           │                     │
│   │  └ Supabase Auth (인증)           │                     │
│   └───────────────────────────────────┘                     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 기술 선택 — 역할별 비교 & 선정 근거

### 1. 웹 앱 호스팅

| 서비스 | 무료 티어 | 장점 | 단점 |
|---|---|---|---|
| **Vercel** ✅ | 100GB 대역폭 | Next.js 최적화, 배포 최고 | 서버리스만, 10초 제한 |
| Netlify | 100GB 대역폭 | Vercel과 유사 | Next.js 지원 Vercel보다 약함 |
| Cloudflare Pages | 무제한 대역폭 | 속도 최고, 무료 관대 | Next.js 호환성 제약 |
| Railway | $5 크레딧/월 | 서버풀 가능, 자유도 높음 | 무료 티어 작음 |
| Fly.io | 3개 VM 무료 | 서버풀, 글로벌 배포 | 설정 복잡 |

**선정: Vercel** — Next.js 쓸 거면 배포·성능·DX 모두 최고. 10초 제한은 스트리밍으로 우회.

### 2. 데이터베이스

| 서비스 | 유형 | 무료 티어 | 장점 | 단점 |
|---|---|---|---|---|
| **Supabase** ✅ | PostgreSQL | 500MB, 무제한 API | Auth 내장, 실시간, REST API 자동 | 무료 프로젝트 2개 |
| Neon | PostgreSQL | 512MB | 서버리스 PostgreSQL, 빠름 | 인지도 낮음 |
| PlanetScale | MySQL | - | 스케일링 좋음 | 무료 티어 폐지됨 |
| Turso | SQLite (클라우드) | 9GB | 엣지에서 빠름 | 생태계 작음 |
| GCP Firestore | NoSQL | 일 5만 읽기 | GCP 통합 | 관계형 쿼리 불편 |
| GCP Cloud SQL | PostgreSQL | **없음** | GCP 통합, 안정적 | 최소 월 $9 |
| MongoDB Atlas | NoSQL | 512MB | 유연한 스키마 | 관계형 데이터에 불편 |

**선정: Supabase** — PostgreSQL + Auth + 무료 티어. 나중에 GCP Cloud SQL로 이전 시 같은 PostgreSQL이라 주소만 바꾸면 됨.

**GCP DB를 안 쓰는 이유:**
- GCP Cloud SQL: 무료 티어 없음 (최소 월 $9)
- GCP Firestore: NoSQL이라 포트폴리오 데이터에 부적합
- 같은 회사 제품이라고 몰아쓰는 게 아니라, 역할별 최적 선택이 정답

### 3. 크롤링/배치 워커

| 서비스 | 무료 티어 | 장점 | 단점 |
|---|---|---|---|
| **GCP Cloud Run** ✅ | 월 200만 요청 | 컨테이너 기반, 자유도 높음 | 콜드 스타트 |
| GCP Cloud Functions | 월 200만 호출 | 더 가벼움, 간단한 작업 | 실행시간 제한 |
| AWS Lambda | 월 100만 요청 | 생태계 큼 | 설정 복잡 |
| GitHub Actions | 월 2,000분 | cron 가능 | 무거운 작업 부적합 |

**선정: GCP Cloud Run + Cloud Scheduler** — Python 크롤러에 최적, 무료 티어 넉넉.

### 4. 인증

| 서비스 | 무료 티어 | 장점 | 단점 |
|---|---|---|---|
| **Supabase Auth** ✅ | 무제한 유저 | DB와 통합, 소셜 로그인 | Supabase 종속 |
| NextAuth.js | 무료 (라이브러리) | 자유도 최고 | 직접 관리 |
| Clerk | 1만 MAU | UI 컴포넌트 제공, 편함 | 유료 전환 시 비쌈 |
| Auth0 | 7,500 MAU | 기업급, 안정적 | 복잡함 |
| Firebase Auth | 무제한 | 구글 계정 연동 쉬움 | GCP 종속 |

**선정: Supabase Auth** — DB와 한 곳에서 해결. 별도 서비스 불필요.

### 5. 결제

| 서비스 | 수수료 | 장점 | 단점 |
|---|---|---|---|
| **Toss Payments** ✅ (국내) | 3.5% | 한국 결제 특화, 간편결제 | 해외 결제 약함 |
| **Stripe** ✅ (글로벌) | 3.4% | 글로벌 표준, 구독 관리 내장 | 한국 정산 느림 |
| 포트원 (구 아임포트) | PG사 수수료 | 여러 PG사 통합 | 직접 관리 많음 |
| Paddle | 5% + $0.50 | 세금/인보이스 자동 | 비쌈 |

**선정: 국내 중심 Toss, 글로벌 확장 시 Stripe**

### 6. AI API

| 서비스 | 무료 티어 | 성능 | 비고 |
|---|---|---|---|
| **Gemini API** ✅ | 분당 15요청 무료 | 상~최상 | 시작에 최적 |
| Claude API (Haiku) | 없음 | 상 | 월 수천원 수준 |
| Claude API (Sonnet) | 없음 | 최상 | 월 만원대 |
| OpenAI (GPT-4o-mini) | 없음 | 상 | 가격 유사 |

**선정: BYOK 방식 — 사용자가 본인 키 입력, 운영 비용 0원**

---

## 규모별 확장 로드맵

### Phase 1: 시작 (0~500명) — 전부 무료

| 서비스 | 플랜 | 비용 |
|---|---|---|
| Vercel | Free | 0 |
| Supabase | Free (500MB) | 0 |
| GCP Cloud Run | Free | 0 |
| GCP Cloud Scheduler | Free (3 job) | 0 |
| yfinance | Free | 0 |
| **합계** | | **0원/월** |

- 그냥 만들고 운영. 아무것도 바꿀 필요 없음.

### Phase 2: 초기 성장 (500~5,000명) — 유료 전환

**전환 트리거:** Supabase 500MB 초과, 상업적 사용 시작

| 서비스 | 변경 | 비용 |
|---|---|---|
| Vercel | → Pro | $20/월 |
| Supabase | → Pro | $25/월 |
| GCP | 유지 | 무료 |
| **합계** | | **~$45/월 (6만원)** |

- 코드 변경 없음, 플랜만 업그레이드
- 구독자 500명 × 2,900원 = 월 145만원 → 운영비 6만원 → 충분히 커버

### Phase 3: 본격 성장 (5,000~50,000명) — 구조 변경 검토

**전환 트리거:** DB 커넥션 한계, 동시 접속 증가, 응답 느려짐

#### 옵션 A: 기존 스택 스케일업 (쉬움, 비쌈)

| 서비스 | 변경 | 비용 |
|---|---|---|
| Vercel | Enterprise | $$$$ |
| Supabase | Team | $599/월 |
| GCP | 유지 | 소액 |
| Upstash Redis | 도입 (캐시) | $10~/월 |

- 코드 변경 최소, 플랜만 올림

#### 옵션 B: GCP로 통합 이전 (어렵지만 저렴) ← 추천

| 서비스 | 변경 | 비용 |
|---|---|---|
| Vercel → GCP Cloud Run | 웹 앱도 GCP로 | $30~/월 |
| Supabase → GCP Cloud SQL | DB 이전 | $50~/월 |
| Redis (Memorystore) | 캐시 도입 | $30~/월 |

- 비용 효율 좋음, DevOps 역량 필요
- **이전이 쉬운 이유: 전부 PostgreSQL이라 데이터 그대로 이동**

```bash
# DB 이전: 명령어 2줄이면 끝
pg_dump supabase_url > backup.sql    # Supabase에서 백업
psql gcp_url < backup.sql            # GCP에 복원

# 코드 변경: 접속 주소 1줄만 변경
DATABASE_URL="supabase_url"  →  DATABASE_URL="gcp_url"
```

### Phase 4: 대규모 (50,000명+) — 풀 인프라

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  CDN (Cloudflare)          ← 정적 파일, 글로벌 캐싱         │
│         │                                                   │
│  로드 밸런서                ← 요청 분산                      │
│    ┌────┴────┐                                              │
│    ▼         ▼                                              │
│  Web 서버 1  Web 서버 2     ← 수평 확장 (오토스케일)         │
│  (Next.js)  (Next.js)                                      │
│    └────┬────┘                                              │
│         ▼                                                   │
│  Redis (캐시)               ← 주가/세션 캐싱, DB 부하 90%↓  │
│         │                                                   │
│         ▼                                                   │
│  PostgreSQL (Primary)       ← 메인 DB                      │
│  + Read Replica 1~N개      ← 읽기 분산                     │
│         │                                                   │
│  메시지 큐 (Cloud Tasks)    ← 크롤링/AI판정/알림 비동기 처리 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

| 서비스 | 비용 |
|---|---|
| GCP Cloud Run (웹) | $50~/월 |
| GCP Cloud SQL + Replica | $100~/월 |
| Redis (Memorystore) | $30~/월 |
| Cloud Tasks + 워커 | $20~/월 |
| Cloudflare CDN | 무료~$20/월 |
| **합계** | **~$200~/월 (30만원)** |

---

## 확장 전략 핵심 원칙

### 1. 처음부터 대규모 설계 ❌ → 갈아타기 쉬운 구조 ✅

```
"처음부터 확장성 있는 구조"가 아니라
"확장할 때 갈아타기 쉬운 구조"가 정답

→ PostgreSQL 선택이 핵심 (어디서든 PostgreSQL은 PostgreSQL)
→ Supabase → GCP Cloud SQL: 주소만 바꾸면 끝
→ Vercel → GCP Cloud Run: Next.js 코드 그대로 Docker로 감싸면 끝
```

### 2. 같은 회사 제품 몰아쓰기 ❌ → 역할별 최적 선택 ✅

```
GCP Cloud Run (크롤링)  → 무료, 최적
GCP Cloud SQL (DB)      → 무료 없음, 비쌈
Supabase (DB)           → 무료, Auth 포함, 최적

역할별로 가성비 좋은 걸 골라 쓰되,
나중에 한 곳으로 통합할 수 있는 표준 기술(PostgreSQL) 사용
```

### 3. 캐시는 나중에 ❌ → Phase 3부터 ✅

```
Phase 1-2: Redis 불필요 (사용자 적음)
Phase 3+:  Redis 도입 → DB 부하 90% 감소

주가 데이터는 하루 1번 업데이트 → 캐시 효과 극대화
5만 명이 "ORCL 현재가" 조회해도 DB는 1번만 읽음
```

### 4. 규모별 비용 vs 수익 추정

| Phase | 사용자 | 구독자 (10%) | 월 수익 | 월 비용 | 순이익 |
|---|---|---|---|---|---|
| 1 | ~500 | ~50 | 145,000 | 0 | 145,000 |
| 2 | ~5,000 | ~500 | 1,450,000 | 60,000 | 1,390,000 |
| 3 | ~50,000 | ~5,000 | 14,500,000 | 300,000 | 14,200,000 |
| 4 | 50,000+ | 5,000+ | 14,500,000+ | 300,000+ | 14,000,000+ |

---

## 로컬 LLM (Gemma) — 선택적 구성

### 용도
- 로컬에서 AI 추론 (인터넷 불필요, 프라이버시)
- 서비스 운영과는 별개, 개인 사용 목적

### PC 사양
- CPU: AMD 9800X3D
- GPU: RTX 5080 (16GB VRAM)
- Gemma 27B 구동 가능

### 셋업
- Ollama + Open WebUI (가장 간단)
- 전기료: 24시간 서버 가동 시 월 ~1~2만원

### 서비스와의 관계
- 서비스 운영에는 사용하지 않음 (클라우드 API가 품질·안정성 우위)
- 개인 분석 작업용으로만 활용
